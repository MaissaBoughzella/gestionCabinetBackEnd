{% extends 'base.html.twig' %}

{% block title %}Imageradio{% endblock %}

{% block body %}
    <h1>Imageradio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ imageradio.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ imageradio.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ imageradio.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('imageradio_index') }}">back to list</a>

    <a href="{{ path('imageradio_edit', {'id': imageradio.id}) }}">edit</a>

    {{ include('imageradio/_delete_form.html.twig') }}
{% endblock %}
