{% extends 'base.html.twig' %}

{% block title %}Analyse{% endblock %}

{% block body %}
    <h1>Analyse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ analyse.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ analyse.nom }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ analyse.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('analyse_index') }}">back to list</a>

    <a href="{{ path('analyse_edit', {'id': analyse.id}) }}">edit</a>

    {{ include('analyse/_delete_form.html.twig') }}
{% endblock %}
