{% extends 'base.html.twig' %}

{% block title %}Typeanalyse{% endblock %}

{% block body %}
    <h1>Typeanalyse</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ typeanalyse.id }}</td>
            </tr>
            <tr>
                <th>Analyse</th>
                <td>{{ typeanalyse.analyse }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('typeanalyse_index') }}">back to list</a>

    <a href="{{ path('typeanalyse_edit', {'id': typeanalyse.id}) }}">edit</a>

    {{ include('typeanalyse/_delete_form.html.twig') }}
{% endblock %}
