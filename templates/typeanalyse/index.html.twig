{% extends 'base.html.twig' %}

{% block title %}Typeanalyse index{% endblock %}

{% block body %}
    <h1>Typeanalyse index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Analyse</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for typeanalyse in typeanalyses %}
            <tr>
                <td>{{ typeanalyse.id }}</td>
                <td>{{ typeanalyse.analyse }}</td>
                <td>
                    <a href="{{ path('typeanalyse_show', {'id': typeanalyse.id}) }}">show</a>
                    <a href="{{ path('typeanalyse_edit', {'id': typeanalyse.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('typeanalyse_new') }}">Create new</a>
{% endblock %}
